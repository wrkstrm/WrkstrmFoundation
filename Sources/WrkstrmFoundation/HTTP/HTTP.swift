/// A namespace for HTTP-related functionality.
enum HTTP {
  /// A namespace for HTTP status codes.
  enum StatusCode: Int {
    /// Represents the HTTP status code for a successful request.
    ///
    /// - 200 OK: The request has succeeded.
    case ok = 200
  }
}
